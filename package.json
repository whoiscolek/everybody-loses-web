{
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "lint": "eslint src api shared scripts tests eslint.config.js",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.js",
    "verify": "npm run lint && npm test && npm run build",
    "admin:manage": "node scripts/manage-admin.mjs"
  },
  "dependencies": {
    "firebase": "12.14.0",
    "firebase-admin": "14.0.0",
    "vite": "8.0.16"
  },
  "name": "everybody-loses-web",
  "version": "10.79.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "globals": "^16.5.0"
  }
}
